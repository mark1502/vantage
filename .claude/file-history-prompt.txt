i'd like to add a feature to the app.  The user should be able to open previously used files without first going to files/index.  So, the app needs to be able to track the recently opened files by user.  When the user opens a file from the file index, it should look to see if that file has been opened by that user before, or within the last 10 files opened.  If not, it should add the file to the list.  Then, as the user hovers over the 'File' link in authenticated user, a droplist would appear so the user can select a file to open, or open the files index.  It seems like we'll need a new table which stores the user_id, the file_id and a last_opened datetime, or something like that.  Then, each time a file is opened, if it's on the list, the last_opened datetime would be updated.  What are your thoughts on how this feature would be best implemented?
